language: python
python:
  - '3.5'

branches:
  only:
    - staging
 
before_script:
  - pip install -r requirements.txt

services:
  - postgresql

env: DJANGO_SETTINGS_MODULE=deployez_une_application_django.settings.travis

script:
  - ./manage.py test
